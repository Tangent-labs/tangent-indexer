query GetProposalVotes($proposalId: String!, $first: Int!, $skip: Int!) {
  votes(first: $first, skip: $skip, where: { proposal: $proposalId }, orderBy: "created", orderDirection: asc) {
    id
    voter
    created
    choice
    vp
    proposal {
      id
      title
    }
  }
}
