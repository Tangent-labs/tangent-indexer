query GetProposalVotes($proposalId: String!) {
  votes(first: 1000, where: { proposal: $proposalId }, orderBy: "created", orderDirection: desc) {
    id
    voter
    created
    choice
    vp
    reason
    proposal {
      id
      title
    }
  }
}
